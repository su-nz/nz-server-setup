# Ubuntu Server Automation Setup 🚀

NZ 的 Ubuntu Server 初始化與自動維護腳本集。

## 📋 功能說明

| 腳本 | 功能 |
|------|------|
| `setup.sh` | 一鍵安裝基礎工具、Docker、Tailscale，配置防火牆與防暴力破解，設定時區與自動更新排程 |
| `update.sh` | 自動化補包與系統清理（每日凌晨 04:00 執行） |

## 🚀 快速開始

在新 Ubuntu 主機上執行以下指令：

```bash
curl -sSL https://raw.githubusercontent.com/su-nz/nz-server-setup/main/setup.sh | bash
```

## 🛠️ 常用指令

| 功能 | 指令 |
|------|------|
| 登入 Tailscale | `sudo tailscale up` |
| 查看更新日誌 | `tail -f /var/log/server_maintenance.log` |
| 手動執行更新 | `sudo /usr/local/bin/update.sh` |
| 防火牆開放連接埠 | `sudo ufw allow 8080/tcp` |
| 查看 Fail2ban 狀態 | `sudo fail2ban-client status` |
| 重啟 Docker | `sudo systemctl restart docker` |

## ⚙️ 安裝內容

### 系統工具
- `htop, net-tools, tmux` - 系統監控與終端工具
- `curl, wget, git, vim` - 基礎開發工具
- `build-essential` - 編譯工具集
- `tree, unzip` - 檔案操作工具

### 安全與容器
- **Docker** - 容器平臺（自動加入 docker 群組，避免 sudo）
- **UFW** - 防火牆（預設開放 SSH 22 端口）
- **Fail2ban** - 防暴力破解
- **Tailscale** - VPN 網路

## ⚙️ 設定說明

- **時區**: 預設設定為 `Asia/Taipei`
- **防火牆**: 自動啟用 UFW，預設開放 SSH 連線
- **防暴力破解**: Fail2ban 已啟用，自動封鎖異常登入嘗試
- **更新模式**: 所有更新均採用 `noninteractive` 模式，避免安裝過程中的詢問視窗
- **定時執行**: 更新任務由 crontab 每日凌晨 04:00 自動執行

## ⚡ 首次使用注意

1. **Docker**: 安裝後需重新登入或執行 `newgrp docker` 才能使用
2. **Tailscale**: 執行 `sudo tailscale up` 登入網路
3. **UFW**: 若需開放其他連接埠，使用 `sudo ufw allow <port>/<protocol>`

## 📝 備註

如需調整時區、防火牆規則或其他設定，請編輯相應的腳本檔案。